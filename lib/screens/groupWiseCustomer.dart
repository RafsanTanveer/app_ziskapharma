import 'package:flutter/material.dart';

class Groupwisecustomer extends StatelessWidget {
  const Groupwisecustomer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}